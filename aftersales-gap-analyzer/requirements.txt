# Core
anthropic>=1.0.0        # Claude API: web search server tool + structured outputs
pydantic>=2.6           # typed stage contracts / JSON schemas
PyYAML>=6.0             # process definitions and the editable filter resources
requests>=2.31          # OpenAlex + page fetch for T-code verification
python-dotenv>=1.0      # local .env for the API key

# Local mode (Ollama + DuckDuckGo). Both are optional:
#   - ollama itself is a separate install (https://ollama.com), not a pip package
#   - ddgs gives a maintained DuckDuckGo client; without it the HTML endpoint is used
ddgs>=9.0

# Reporting (optional but installed by default - DOCX export)
python-docx>=1.1

# Development
pytest>=8.0
